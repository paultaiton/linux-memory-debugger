{
    "tasks": [
        {
            "label": "C/C++: gcc build active file",
            "command": "/usr/bin/make",
            "args": [
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "make build"
        }
    ],
    "version": "2.0.0"
}

